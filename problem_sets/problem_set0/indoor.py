# Prompt the user for input and store it in the 'message' variable
message = input("").lower().strip()

# Convert the input message to lowercase using the .lower() method
# and remove leading/trailing whitespace using the .strip() method

# Print the modified message
print(message)
